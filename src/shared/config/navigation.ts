// Re-export from navigation.config for simpler imports
export { navigationConfig, sectionIds } from './navigation.config';
export type { NavItem, SectionId } from './navigation.config';

// Alias for components expecting NAV_ITEMS
export { navigationConfig as NAV_ITEMS } from './navigation.config';
